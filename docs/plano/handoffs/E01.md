# Handoff E01 — Neon + Auth + staff

**Status:** Concluído

## Feito

- Projeto Neon `bancotalentos` (super-frost-69304525) em aws-us-east-1
- Managed Better Auth (`better_auth`) provisionado
- Next.js 16 scaffold com App Router, Tailwind, shadcn
- `@neondatabase/auth` + middleware + `/api/auth/[...path]`
- Tabela `staff_users` via migration Drizzle
- Gate: domínio escola + allowlist em `src/lib/auth/staff.ts`
- Seed admin: `luis.ribeiro@liceujardim.pro.br`

## Validar

```bash
npm run build
curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/auth/sign-in  # 200
```

## Próxima

E02 — schema completo (já aplicado na mesma migration inicial)
